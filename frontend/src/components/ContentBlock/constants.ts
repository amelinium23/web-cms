const titleClassName = "text-2xl font-bold text-[#055F94]";

const imageContainerClassName =
  "flex w-full p-4 flex-col gap-4 items-center justify-center flex-wrap";

const textContainerClassName =
  "flex w-full justify-center p-12 rounded-3xl flex-col gap-5 flex-wrap";

const shadowStyle = {
  boxShadow:
    "0px 5px 15px rgba(0, 0, 0, 0.25), inset 0px 5px 15px rgba(255, 255, 255, 0.25)",
};

export {
  titleClassName,
  imageContainerClassName,
  textContainerClassName,
  shadowStyle,
};
